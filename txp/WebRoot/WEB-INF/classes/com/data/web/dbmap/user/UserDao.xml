<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.data.web.dao.user.UserDao">
    <resultMap type="com.data.web.model.user.UserModel" id="UserModel">
        <result property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="passWord" column="password"/>
        <result property="roleType" column="role_type"/>
        <result property="classId" column="class_id"/>
    </resultMap>

    <select id="findByUserName" parameterType="java.lang.String" resultMap="UserModel">
        select id, user_name as userName,password as passWord, role_type as roleType, class_id as classId  from users where is_delete = 0 and user_name = #{userName}
    </select>


    <insert id="add" parameterType="com.data.web.model.user.UserModel" >
    <selectKey keyProperty="id" resultType="String" order="BEFORE">
    	select uuid() from dual
    </selectKey>
        insert into users
        (
        id,
        user_name,
        password,
        role_type,
        class_id,
        created,
        modified,
        is_delete,
        remark
        )
        values
        (
        #{id},
        #{userName,jdbcType=VARCHAR},
        #{passWord,jdbcType=VARCHAR},
        #{roleType,jdbcType=INTEGER},
        #{classId,jdbcType=VARCHAR},
        #{created,jdbcType=TIMESTAMP},
        #{modified,jdbcType=TIMESTAMP},
        #{isDelete,jdbcType=INTEGER},
        #{remark,jdbcType=VARCHAR}
        )
    </insert>

    <update id="update" parameterType="com.data.web.model.user.UserModel">
        update users set
        password=#{passWord,jdbcType=VARCHAR},
        modified=#{modified,jdbcType=TIMESTAMP}
        where id=#{id}
    </update>

</mapper> 